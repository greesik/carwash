import React from 'react';
import "./Header.css";

const Header = () => {
    return (
        <header className={"header"}>
            <div className={"header-container"}>
                Header
            </div>
        </header>
    );
};

export default Header;