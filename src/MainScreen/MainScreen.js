import React from 'react';
import "./MainScreen.css";

const MainScreen = () => {
    return (
        <section className={"main-screen"}>
            MainScreen
        </section>
    );
};

export default MainScreen;